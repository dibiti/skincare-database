# Copy this file to .env and adjust if needed.
# These are local development credentials only — never reuse real passwords here.
POSTGRES_USER=skincare
POSTGRES_PASSWORD=change-me-local-dev
POSTGRES_DB=skincare_db
POSTGRES_HOST_PORT=5433
